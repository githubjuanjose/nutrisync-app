/**
 * Rama WEB (y por defecto) del cargador de Health Connect: no hay nada que
 * cargar. La pareja `.native.ts` de al lado trae el módulo real en Android.
 */
export function cargaHC(): Record<string, any> | null {
  return null;
}
